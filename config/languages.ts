import { Language } from '@/lib/translations';

export const languages: Language[] = ['en', 'hi', 'ta'];

export const languageNames: Record<Language, string> = {
  en: 'English',
  hi: 'हिंदी',
  ta: 'தமிழ்',
}; 